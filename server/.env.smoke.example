# 仅隔离测试。复制为 .env.smoke，权限设为600，禁止提交或发送真实值。
# 分别生成两个不同的至少32字节随机密码，推荐64位十六进制字符。
DB_ADMIN_PASSWORD=replace-with-independent-random-admin-password
DB_APP_PASSWORD=replace-with-independent-random-app-password
WECHAT_APP_SECRET=replace-with-real-wechat-app-secret

# 用户已确认以下 ECR 镜像下载成功；应用构建和数据库启动仍待验证。
# 测试阶段标签可变；验收/正式发布应记录并固定仓库的 sha256 摘要。
NODE_IMAGE=public.ecr.aws/docker/library/node:24-bookworm-slim
POSTGRES_IMAGE=public.ecr.aws/docker/library/postgres:17-bookworm
